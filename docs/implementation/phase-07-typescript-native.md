# Phase 07 Plan: TypeScript Client And Native Binding

Add `engram-node` and `packages/node` after core behavior stabilizes. Keep
`packages/client` as a narrow facade over generated contracts and native calls.
