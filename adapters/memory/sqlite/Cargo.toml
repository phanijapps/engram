[package]
name = "engram-store-sql"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
# Shim: the memory code + its tests/examples have folded into engram-store-sqlite
# (consolidation T1). This crate only re-exports it so existing
# `engram_store_sql::*` consumers keep compiling until re-pointed (T7) and this
# crate is deleted (T8).
engram-store-sqlite = { path = "../../sqlite" }
