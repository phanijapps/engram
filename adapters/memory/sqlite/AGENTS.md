# SQLite Memory Adapter Instructions

Read ../../../AGENTS.md and ../../AGENTS.md first.

This crate owns SQLite-backed memory persistence behind memory ports.

Do not add knowledge graph storage, ontology storage, vector indexing,
embedding/model providers, TypeScript bindings, or orchestration policy that
belongs in core crates.
