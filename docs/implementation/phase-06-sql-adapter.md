# Phase 06 Plan: SQL Persistence Adapter

SQLite is accepted as the first SQL engine in ADR 0006. The current slice
implements schema initialization plus memory/event repository ports. Next work
is SQL-backed `MemoryService` orchestration and fixture conformance.
